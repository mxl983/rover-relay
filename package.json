{
  "name": "rover-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Rover mission-control dashboard (drive + WebRTC). Talks directly to the Pi over Tailscale.",
  "scripts": {
    "dev": "npm --prefix control-dashboard run dev",
    "build": "npm --prefix control-dashboard run build",
    "preview": "npm --prefix control-dashboard run preview",
    "test": "npm --prefix control-dashboard run test",
    "lint": "npm --prefix control-dashboard run lint"
  }
}
