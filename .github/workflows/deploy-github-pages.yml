# Publishes control-dashboard to GitHub Pages using the official Actions flow.
# Repo → Settings → Pages → Build and deployment → Source: GitHub Actions.
# Deploys from liyang-dashboard on every push that touches the dashboard (or this workflow).
name: Deploy GitHub Pages

on:
  push:
    branches: [liyang-dashboard]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: control-dashboard/package-lock.json

      - name: Install & build (GitHub Pages base path)
        working-directory: control-dashboard
        env:
          VITE_BASE_PATH: /rover-relay/
        run: |
          npm ci
          npm run build:github-pages

      - uses: actions/upload-pages-artifact@v3
        with:
          path: control-dashboard/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
